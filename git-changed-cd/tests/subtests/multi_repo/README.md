# Multi-Repository Tests

Tests for multi-repository navigation:

## Modes (`modes/`)
- `--justRegisteredDirectories` mode
- `--all` mode  
- Empty registry handling

## Navigation (`navigation/`)
- Sequential numbering across repositories
- Cross-repository navigation
